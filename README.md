# Humanity_OpenProjects

This repository now contains an initial scaffold for a **Physics Data Platform**:

- A local-first **backend API** for multi-user notebook storage + sharing (see `backend/`)
- A planned UI concept for **multi-pane** viewing (graphs, data tables, paper breakdowns) that can later connect to the backend

## Backend quickstart

See `backend/README.md`.

